package com.example.javaapk.activities.menu;

import android.content.Intent;
import android.os.Bundle;
import android.widget.Button;
import android.widget.ImageView;
import android.widget.LinearLayout;
import android.widget.TextView;
import android.widget.Toolbar;

import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;
import androidx.fragment.app.Fragment;
import androidx.fragment.app.FragmentManager;
import androidx.fragment.app.FragmentTransaction;

import com.example.javaapk.R;
import com.example.javaapk.data.DataManager;
import com.example.javaapk.data.Profile;


//the activity the user uses to select a profile.
public class ProfilesActivity extends AppCompatActivity {

    //androidx.appcompat.widget.Toolbar useDifferentAccountBtn;
    public Toolbar differentAccount;
    public LinearLayout linearLay;
    public ImageView imageView;
    public TextView textView;

    Button addProfileButton;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        //autogenerated
        super.onCreate(savedInstanceState);
        setContentView(R.layout.aactivity_login);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.loginMain), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });

        addProfileButton = findViewById(R.id.addProfileButtonnn);
        addProfileButton.setOnClickListener(v -> addNewProfileAndSelect());

        DataManager dataManager = DataManager.getInstance();
        for(Profile profile : dataManager.profiles){
            addProfileToDisplay(profile);
        }


        /*Toolbar differentAcconut = findViewById(R.id.different_account);
        ScrollView loginScrollView = findViewById(R.id.login_scroll_view);
        Toolbar jinyUcetLayout = findViewById(R.id.jiny_ucet_layout);
        Toolbar useDifferentAccountBtn = findViewById(R.id.different_account);

        useDifferentAccountBtn.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                jinyUcetLayout.setVisibility(View.VISIBLE);

                loginScrollView.setEnabled(false);
                differentAcconut.setEnabled(false);

            }
        });
        ImageButton closeDifferentAccount = findViewById(R.id.close_layout);
        closeDifferentAccount.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                jinyUcetLayout.setVisibility(View.GONE);

                loginScrollView.setEnabled(true);
                differentAcconut.setEnabled(true);
            }
        });*/

        linearLay = findViewById(R.id.linearLay);
        imageView = findViewById(R.id.imageView);
        textView = findViewById(R.id.textViewJinyUcet);
        differentAccount = findViewById(R.id.puzit_jiny_ucet_btn);
        differentAccount.setOnClickListener(v -> loadFragment(new DifferentAccountFragment()));
        textView.setOnClickListener(v -> loadFragment(new DifferentAccountFragment()));
        imageView.setOnClickListener(v -> loadFragment(new DifferentAccountFragment()));
        linearLay.setOnClickListener(v -> loadFragment(new DifferentAccountFragment()));

        //TODO: remove this after debugging:
        addNewProfileAndSelect();

    }

    private void loadFragment(Fragment fragment) {
        FragmentManager fragmentManager = getSupportFragmentManager();
        FragmentTransaction transaction = fragmentManager.beginTransaction();
        transaction.replace(R.id.loginMain, fragment);
        transaction.addToBackStack(null);
        transaction.commit();
    }

    void selectProfile(int profileIndex){
        if(DataManager.getInstance().profiles.get(profileIndex).isPasswordRemembered()){
            DataManager.getInstance().selectProfile(profileIndex);
            this.finish();
        }else{
            Intent intent = new Intent(this, LoginIntoProfileActivity.class);
            intent.setAction(LoginIntoProfileActivity.ACTION_LOGIN_EXISTING);
            intent.putExtra("profileIndex", profileIndex);
            startActivity(intent);
            finish();
        }
    }
    void addProfileToDisplay(Profile profile){
        //add an icon displaying this profile as available to login as.
        //add a listener for the profile display so that it selects it after clicking on it.
    }
    void addNewProfileAndSelect(){
        Intent intent = new Intent(this, LoginIntoProfileActivity.class);
        intent.setAction(LoginIntoProfileActivity.ACTION_LOGIN_NEW);
        startActivity(intent);
        finish();
    }

}